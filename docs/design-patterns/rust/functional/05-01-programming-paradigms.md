# Programming paradigms

Imperative vs declarative styles. Example: summing 1..10 imperatively vs using `(1..11).fold(0, |a,b| a + b)`.

Discussion

Rust supports expression-oriented, functional programming with iterators, `map`/`filter`/`fold`, and
pattern matching.

Last change: 2026-01-03, commit:f279f35
